# FilmTrack v0.5.1 更新日志

## 🐛 问题修复

### 命令面板与本地存储修复
- 去掉 `Cmd/Ctrl + K` 命令面板背后的黑色遮罩与背景模糊，保留点击空白关闭的交互
- 修复 `localStorage` 不可用或接口不完整时的报错噪音，测试环境会静默回退到默认值

## 🎨 体验优化

### 路由与快捷操作一致性优化
- 统一记录页、详情页、导入页路由为小写地址，减少大小写混用带来的跳转分歧
- 保留旧大写地址兼容重定向，已有书签或历史链接仍可正常打开

## 🔧 工程维护

### 发布链路与校验补齐
- 恢复 `npm run lint` 到项目内 ESLint 依赖与 flat config，避免依赖 `npx` 临时拉取
- 补充命令面板、路由兼容、存储回退与发版脚本相关测试，降低后续回归风险

**发布日期**: 2026-06-08
**版本号**: v0.5.1
